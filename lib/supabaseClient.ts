// Supabase client helpers are present but disabled in offline prototype.
// TODO: Restore and secure these clients when enabling real backend.
// The file remains to avoid import errors elsewhere; do not call these in offline mode.
export const clientSideSupabase = null;
export const serverClient = null;
